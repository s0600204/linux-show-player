[DEFAULT]
pristine-tar = False
sign-tags = False

upstream-tree = BRANCH
upstream-branch = dev_master
debian-branch = debian-develop

upstream-tag = %(version)s
debian-tab = local/%(version)s

export-dir = debian/build

